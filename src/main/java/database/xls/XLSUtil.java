package database.xls;

import model.Module;
import model.StandardQuestion;
import model.enums.YesOrNoAnswer;
import org.apache.poi.ss.usermodel.Cell;
import org.apache.poi.ss.usermodel.Row;

/**
 * Author: Daniel
 * Date: 05.11.13
 */
public class XLSUtil {
  /* public static StandardQuestion getStandardQuestionFromRow(Row row) {

        *//*Module module = null;
        String picturePath = null;
        String videoPath = null;

        for (int column = 0; column < 2; column++) {
            Cell tempCell = row.getCell(column);

             int points = (int) row.getCell(3).getNumericCellValue();
             String question = row.getCell(4).getStringCellValue();
             YesOrNoAnswer correctAnser = YesOrNoAnswer.valueOf(row.getCell(8).getStringCellValue());
             mo

            }
        }*//*
   }*/

}
